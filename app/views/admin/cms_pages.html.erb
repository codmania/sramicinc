<!--start search-container-->
<div class="container-fluid search-container">
  <div class="container">
    <div class="row">

      <div class="col-lg-12 no_pad ">

        <div class="company_users">

          <h1> CMS Pages </h1>

          <div id="no-more-tables">
            <table class="col-sm-12 table-striped table-condensed cf">
              <thead class="cf">
              <tr>
                <th width="400">Page Name</th>
                <th width="100" class="table-last-th"></th>
              </tr>
              </thead>
              <tbody>
              <% @pages.each do |page| %>
                  <tr>
                    <td data-title="Page Name"><%= page.name %></td>
                    <td data-title="" class="table-last-td">

                      <ul class="actions_container">
                        <li>
                          <%= link_to '<span class="icon icon_1"></span>'.html_safe,
                                      page_path(page) %>

                        </li>
                      </ul>

                    </td>
                  </tr>
              <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>

    </div>

  </div>

</div>
