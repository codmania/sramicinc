<%= form_for @category, :html => { :class => "category" } do |f| %>

  <% if @category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@category.errors.count, "error") %> prohibited this job from being saved:</h2>
    <ul>
      <% @category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
      <% end %>
    </ul>
  </div>
  <% end %>


  <div class="form-group">
    <label>Category Name<em>*</em></label>
    <%= f.text_field :name ,:required=>true%>
  </div>
  
  <div class="actions">
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),categories_path, :class => 'btn btn-ghost' %>
    <%= f.submit 'Save', :class => 'btn btn-blue' %>
  </div>

<%end%>


