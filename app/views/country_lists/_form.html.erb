<%= form_for @country_list, :html => { :class => "country_list" } do |f| %>

  <% if @country_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@country_list.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
        <% @country_list.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <label>Country Name<em>*</em></label>
    <%= f.text_field :name ,:required=>true%>
  </div>

  
  <div class="actions">
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),country_lists_path, :class => 'btn btn-ghost' %>
    <%= f.submit 'Save', :class => 'btn btn-blue' %>
  </div>

<%end%>

