<%= form_for @enquiry_category, :html => { :class => "enquiry_category" } do |f| %>

  <% if @enquiry_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@enquiry_category.errors.count, "error") %> prohibited this enquiry category from being saved:</h2>

      <ul>
        <% @enquiry_category.errors.full_messages.each do |message| %>
            <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <label>Name<em>*</em></label>
    <%= f.text_field :name ,:required=>true%>
  </div>

  
  <div class="actions">
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),enquiry_categories_path, :class => 'btn btn-ghost' %>
    <%= f.submit 'Save', :class => 'btn btn-blue' %>
  </div>

<%end%>


