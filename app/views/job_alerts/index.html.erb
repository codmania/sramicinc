<div class="inboard">
  <div class="margin">

    <div class="enclosure">
      <h3>Job Alert</h3>
      <h6><%= link_to "Add", new_job_alert_path %></h6>

      <table width="100%" border="0" cellspacing="0" cellpadding="0" class="lodge">
        <tr>
          <th>What</th>
          <th>Where</th>
          <th>Within</th>
          <th class="last">Actions</th>
        </tr>
        <% @job_alerts.each do |job_alert| %>
        <tr>
          <td data-title="what"><span><%= job_alert.what %></span></td>
          <td data-title="where"><span><%= job_alert.where %></span></td>
          <td data-title="within"><span><%= job_alert.within %></span></td>
          <td class="last">
 

            <%= link_to edit_job_alert_path(job_alert), class: "icon icon-edit", title: "Edit" do %><% end %>
            <%= link_to job_alert_path(job_alert), :method => :delete, :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, class: "icon icon-trash", title: "Delete" do %><% end %>
          </td>
        </tr>
        <% end %>
      </table> 

    </div>



  </div>
</div>