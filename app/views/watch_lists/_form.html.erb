<%= form_for(@watch_list) do |f| %>
  <% if @watch_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@watch_list.errors.count, "error") %> prohibited this watch_list from being saved:</h2>

      <ul>
      <% @watch_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :job_id %><br>
    <%= f.number_field :job_id %>
  </div>
  <div class="field">
    <%= f.label :jobseeker_id %><br>
    <%= f.number_field :jobseeker_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
